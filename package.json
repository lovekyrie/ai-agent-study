{
  "name": "ai-agent-study",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "AI Agent 学习项目 - 从基础到生产级完整实现",
  "author": "",
  "license": "MIT",
  "keywords": [
    "ai",
    "agent",
    "llm",
    "rag",
    "mcp",
    "typescript"
  ],
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter stage01-llm-api dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "clean": "rm -rf node_modules dist packages/*/node_modules stages/*/node_modules projects/*/node_modules",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^7.0.0",
    "@types/node": "^22.0.0",
    "@vitest/ui": "^2.0.0",
    "eslint": "^9.10.0",
    "tsx": "^4.15.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
